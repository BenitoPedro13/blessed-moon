/**
 * Confirmed production URL (per user, current Vercel deployment). Single
 * source so a future custom domain is a one-line change here rather than a
 * hunt across metadata/robots/sitemap files. CLAUDE.md's "deployment
 * target: open question" still applies to anything beyond this.
 */
export const SITE_URL = "https://blessed-moon.vercel.app";
